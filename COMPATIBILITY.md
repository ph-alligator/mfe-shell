# Shell ↔ Remote compatibility

| Shell version | dashboard remote | orders remote |
|---------------|------------------|---------------|
| 0.1.0         | >= 0.1.0         | >= 0.1.0      |

Remote URL configured via `public/manifest.json` or `VITE_REMOTE_DASHBOARD_URL`.
